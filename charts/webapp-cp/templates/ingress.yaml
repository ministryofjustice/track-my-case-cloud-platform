{{ if .Values.Ingress.Enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ template "stripPrefixSuffixFromName" . }}
  annotations:
  {{- with .Values.Ingress.rateLimit }}
    nginx.ingress.kubernetes.io/limit-req-status-code: "429"
  {{- if .rps }}
    nginx.ingress.kubernetes.io/limit-rps: {{ .rps | quote }}
  {{- end }}
  {{- if .rpm }}
    nginx.ingress.kubernetes.io/limit-rpm: {{ .rpm | quote }}
  {{- end }}
  {{- if .connections }}
    nginx.ingress.kubernetes.io/limit-connections: {{ .connections | quote }}
  {{- end }}
  {{- if .multiplier }}
    nginx.ingress.kubernetes.io/limit-burst-multiplier: {{ .multiplier | quote }}
  {{- end }}
  {{- if .whitelist }}
    nginx.ingress.kubernetes.io/limit-whitelist: {{ .whitelist | quote }}
  {{- end }}
  {{- end }}
  {{ if .Values.Ingress.ModSec.Enabled }}
    nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    nginx.ingress.kubernetes.io/modsecurity-snippet: |
  {{- if .Values.Ingress.ModSec.DetectionOnly }}
      SecAuditEngine On
      SecRuleEngine DetectionOnly
  {{- else }}
      SecRuleEngine On
  {{- end }}
      SecDefaultAction "phase:2,pass,log{{ if .Values.GithubTeam }},tag:github_team={{ .Values.GithubTeam }}{{ end }}"
  {{ end }}
    nginx.ingress.kubernetes.io/affinity: "cookie"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Content-Type-Options: nosniff";
      more_set_headers "X-Frame-Options: deny";
      {{ if .Values.Ingress.Headers.SameOrigin }}
      more_set_headers "X-Frame-Options: SAMEORIGIN";
      {{ end }}
    external-dns.alpha.kubernetes.io/set-identifier: "{{ template "stripPrefixSuffixFromName" . }}-{{ .Values.Namespace }}-green"
    external-dns.alpha.kubernetes.io/aws-weight: "100"
  {{- with .Values.Ingress.ExtraAnnotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{ if .Values.Ingress.ModSec.Enabled }}
  ingressClassName: {{ .Values.Ingress.ModSec.Class }}
  {{ else }}
  ingressClassName: default
  {{ end }}
  tls:
    - hosts:
        - {{ template "stripPrefixSuffix" . }}
  rules:
    - host: {{ template "stripPrefixSuffix" . }}
      http:
        paths:
        {{- if kindIs "slice" .Values.Ingress.Paths }}
          {{- range .Values.Ingress.Paths }}
            - pathType: ImplementationSpecific
              path: {{ . | quote }}
              backend:
                service:
                  name: {{ $.Values.WebApp.Name }}
                  port:
                    number: {{ $.Values.Ingress.Port }}
          {{- end }}
        {{- else if .Values.Ingress.Path }}
            - pathType: ImplementationSpecific
              path: {{ .Values.Ingress.Path | quote }}
              backend:
                service:
                  name: {{ $.Values.WebApp.Name }}
                  port:
                    number: {{ $.Values.Ingress.Port }}
        {{- else }}
            - pathType: ImplementationSpecific
              path: "/"
              backend:
                service:
                  name: {{ $.Values.WebApp.Name }}
                  port:
                    number: {{ $.Values.Ingress.Port }}
        {{- end }}
{{ end }}
